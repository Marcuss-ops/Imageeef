# Piano: A+B+C — Bug Critici

## Branch: `codex/fix-critical-claims`

### A. Wiring RequeueZombieJobs nel startup
**File**: `DataServer/internal/bootstrap.go` o il server main
- Aggiungere un goroutine che chiama `fileQ.RequeueZombieJobs()` ogni 60 secondi
- Log quanti job sono stati requeued
- Testare che i job scaduti vengono effettivamente rimessi in PENDING

### B. Fix job_type passing in compatibility check
**File**: `DataServer/internal/services/jobs/service.go`
- Aggiungere campo `JobType string` alla struct `ClaimRequest` (line ~24)
- In `ClaimNextJob`, passare `req.JobType` a `checkWorkerCompatibility` invece di `""`
- Verificare che i job di tipo non supportato vengano rifiutati

### C. Aggiungere bundle_hash check in compatibility
**File**: `DataServer/internal/services/jobs/worker_compatibility.go`
- Leggere il bundle hash corrente dal manifest o dalla config del server
- Confrontare con `worker.BundleHash`
- Se diverso → reject con reason chiara
- Testare che un worker con bundle_hash sbagliato non riceva job

## Verifica

```bash
cd refactored/DataServer
go test ./internal/queue ./internal/services/jobs ./internal/store ./internal/handlers/server/jobs
go build ./cmd/server
```
