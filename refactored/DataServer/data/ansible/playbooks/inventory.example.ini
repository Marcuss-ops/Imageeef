[workers]
# ─────────────────────────────────────────────────────────────
# Inventory example — copy to inventory.ini and fill in values
# DO NOT commit inventory.ini (it is in .gitignore)
# ─────────────────────────────────────────────────────────────

host_57_129_132_133 ansible_host=TAILSCALE_IP ansible_user=pierone
host_149_56_131_97  ansible_host=TAILSCALE_IP ansible_user=pierone
host_51_222_204_158 ansible_host=TAILSCALE_IP ansible_user=pierone

[workers:vars]
# Prefer SSH keys over passwords. Set ssh_private_key_file if needed.
# ansible_ssh_private_key_file=~/.ssh/id_ed25519
# ansible_become_pass=VAULT_ENCRYPTED_PASSWORD
